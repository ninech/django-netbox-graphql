sudo: required
language: python

services:
  - docker

install:
  - docker-compose pull --parallel
  - docker-compose build

script:
  - docker-compose run netbox ./manage.py test

after_script:
  - docker-compose down
  