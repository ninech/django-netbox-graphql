sudo: required
language: python

services:
  - docker

install:
  - docker-compose -f docker-compose.test.yml pull --parallel
  - docker-compose -f docker-compose.test.yml build

script:
  - docker-compose -f docker-compose.test.yml run netbox

after_script:
  - docker-compose -f docker-compose.test.yml down